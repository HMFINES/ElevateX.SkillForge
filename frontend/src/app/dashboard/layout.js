export const metadata = {
  title: "Dashboard",
  description: "Manage progress, certificates, recommendations, and your ElevateX plan.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function DashboardLayout({ children }) {
  return children;
}
