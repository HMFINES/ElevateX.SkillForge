export const metadata = {
  title: "Certificates",
  description:
    "Access your downloadable ElevateX certificates and share verifiable proof of learning.",
};

export default function CertificatesLayout({ children }) {
  return children;
}
