export const metadata = {
  title: "Verify Certificate",
  description:
    "Publicly verify ElevateX certificates with their unique certificate ID.",
};

export default function VerifyLayout({ children }) {
  return children;
}
