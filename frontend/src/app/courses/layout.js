export const metadata = {
  title: "Courses",
  description:
    "Browse ElevateX learning tracks, certificate-ready internal courses, and curated external recommendations.",
};

export default function CoursesLayout({ children }) {
  return children;
}
